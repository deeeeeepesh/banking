"use client"
import React from 'react'
import {MobileNavProps} from '../constants/index'

const MobileNav = ({user}:MobileNavProps) => {
  return (
    <div>MobileNav</div>
  )
}

export default MobileNav